# Copy to .env and fill in real values. .env is gitignored.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vanprastha?schema=public"
JWT_SECRET="replace-with-at-least-32-random-characters"
ADMIN_EMAIL="admin@vanprastha.com"
ADMIN_PASSWORD="replace-with-strong-password"
# Razorpay test/live keys. Leave empty to disable payments (booking still works, payment step shows unavailable).
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
