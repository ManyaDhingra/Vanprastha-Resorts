# Copy to .env and fill in real values. .env is gitignored.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vanprastha?schema=public"
JWT_SECRET="replace-with-at-least-32-random-characters"
ADMIN_EMAIL="admin@vanprastha.com"
ADMIN_PASSWORD="replace-with-strong-password"
# Razorpay test/live keys. Leave empty to disable payments (booking still works, payment step shows unavailable).
# NEXT_PUBLIC_RAZORPAY_KEY_ID is the publishable key — safe to expose client-side; required for the checkout modal.
# RAZORPAY_WEBHOOK_SECRET signs server-to-server events (payment.captured / refund.processed). Leave empty to
# disable webhooks (client-verify still works, but lost captures have no fallback).
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
NEXT_PUBLIC_RAZORPAY_KEY_ID=""
RAZORPAY_WEBHOOK_SECRET=""
